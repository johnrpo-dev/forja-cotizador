[project]
name = "forja"
version = "0.1.0"
description = "Generador de documentos comerciales para pymes — Reto R3, Maratón de IA Ruta N"
requires-python = ">=3.10"
dependencies = ["python-docx>=1.2.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
